# It will not override the system variables

# API
export FLASK_APP=autoapp.py
export FLASK_DEBUG=1
export FLASK_ENV=development

export APP_HOST=0.0.0.0
export APP_PORT=5000

export DB_HOST=127.0.0.1
export DB_NAME=knotdb
export DB_PORT=5432
export DB_USER=postgres
export DB_SSL=disable
export DB_PASSWORD=
# travis postgresql doesn't need
# db password but you have to supply
# it when you test locally

export KAFKA_HOST=127.0.01
export KAFKA_PORT=9092
export RESTKNOT_KAFKA_TOPIC=domaindata
export DEFAULT_NS='satu.neodns.id. dua.neodns.id.'
export DEFAULT_SOA_RDATA='satu.neodns.id. hostmaster.neodns.id. 10800 3600 604800 38400'
export RESTKNOT_CLUSTER_FILE=api/tests/functional/servers.yaml
export RESTKNOT_API_KEY=123
