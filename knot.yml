configbegin:
  sendblock:
    cmd: conf-begin
  receive:
    type: block

configset:
  sendblock:
    cmd: conf-set
    section: zone
    item: domain
    data: tes123
  receive:
    type: block

configcommit:
  sendblock:
    cmd: conf-commit
  receive:
    type: block

statsserver:
  sendblock:
    cmd: stats
  receive:
    type: stats
