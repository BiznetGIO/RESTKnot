project-dns:
  command:
    block_command:
      cmd: conf-begin
    block_command:
      cmd: conf-set
      section: zone
      item: domain
      data: tes123
    block_command:
      cmd: conf-commit
    block_command:
      cmd: conf-read
      section: zone
      item: domain