endpoint:
  create:
    command:
      identifier1:
        sendblock:
          cmd: example command
        receive:
          type: block

      identifier2:
        sendblock:
          cmd: example command
          section: example zone
          item: example domain
          data:
            - name: Example Name
            - provider: example provider
        receive:
          type: block

  update:
    command:
      identifier1:
        sendblock:
          cmd: example command
        receive:
          type: block

      identifier2:
        sendblock:
          cmd: example command
          section: example zone
          item: example domain
          data:
            - name: Example Name
            - provider: example provider
        receive:
          type: block
      sendblock:
        cmd: conf-commit
      receive:
        type: block

  delete:
    command:
      identifier1:
        sendblock:
          cmd: example command
        receive:
          type: block

      identifier2:
        sendblock:
          cmd: example command
          section: example zone
          item: example domain
          data:
            - name: Example Name
            - provider: example provider
        receive:
          type: block