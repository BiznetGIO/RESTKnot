zone-begin:
  sendblock:
    cmd: zone-begin
    zone: rosa1.com
  receive:
    type: block

zone-set:
  sendblock:
    cmd: zone-set
    zone: rosa1.com
    owner: lagi
    rtype: A
    ttl: "86400"
    data: "8.8.8.8"
  receive:
    type: block

zone-commit:
  sendblock:
    cmd: zone-commit
    zone: rosa1.com
  receive:
    type: block
