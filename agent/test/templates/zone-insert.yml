zone-begin:
  sendblock:
    cmd: zone-begin
    zone: rosa.com
  receive:
    type: block

zone-set:
  sendblock:
    cmd: zone-set
    zone: rosa.com
    owner: rosa
    rtype: A
    ttl: "300"
    data: "1.2.3.4"
  receive:
    type: block

zone-commit:
  sendblock:
    cmd: zone-commit
    zone: rosa.com
  receive:
    type: block