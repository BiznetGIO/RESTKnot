configbegin:
  sendblock:
    cmd: conf-begin
    data: server
  receive:
    type: block

configset:
  sendblock:
    cmd: conf-set
    section: zone
    item: domain
    data: test123
  receive:
    type: block

configcommit:
  sendblock:
    cmd: conf-commit
  receive:
    type: block

configread:
  sendblock:
    cmd: conf-read
    section: zone
    item: domain
  receive:
    type: block